# Auto-provisioned Prometheus datasource for GymTrack Grafana.
# Managed by provisioning — set editable: false so UI changes don't drift from repo.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    uid: prometheus
